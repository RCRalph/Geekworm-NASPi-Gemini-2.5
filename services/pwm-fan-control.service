[Unit]
Description=Modulate PWM fan speed
After=network.target

[Service]
EnvironmentFile=/etc/geekworm-scripts.conf
ExecStart=/bin/bash -c "$GEEKWORM_SCRIPTS_PATH/pwm_fan_control"
Type=simple
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
